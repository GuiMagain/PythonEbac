# def soma(x, y):
#     return x + y

# print(soma(2, 3))

# soma = lambda x, y: x + y
# print(soma(2, 3))

numeros = [1, 2, 3, 4, 5]

duplicados = list(map(lambda numero: numero * 2, numeros))

print(list(duplicados))
