[project]
name = "projectpy"
version = "0.1.0"
description = ""
authors = [
    {name = "Guilherme ",email = "guimbrum@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard] (>=0.139.0,<0.140.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
